@model DoctorAppointmentManagement.Data.ApplicationUser

@using DoctorAppointmentManagement.Data
@{
    Layout = "AdminLayout";
    ViewData["Title"] = "Edit";
}

<section class="card">
    <form method="post" asp-action="EditUser" class="p-4" >
        <div class="card-header">
            <h2>Create User</h2>
        </div>

        <div asp-validation-summary="All"></div>

        <div class="mb-3">
            <label asp-for="Name" class="form-label">Doctor Name</label>
            <input asp-for="Name" class="form-control" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="UserName" class="form-label">UserName</label>
            <input asp-for="UserName" class="form-control" />
            <span asp-validation-for="UserName" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Email" class="form-label">Email</label>
            <input asp-for="Email" class="form-control" />
            <span asp-validation-for="Email" class="text-danger"></span>
        </div>

       @*  <div class="form-group">
            <label asp-for="ProfilePictureFile" class="control-label"></label>
            <input asp-for="ProfilePictureFile" type="file" value="Upload" class="form-control" />
            <span asp-validation-for="ProfilePictureFile" class="text-danger"></span>

            @if (!string.IsNullOrEmpty(Model?.ProfilePicture))
            {
                <div class="mt-2">
                    <img src="@Model.ProfilePicture" alt="Profile Picture" style="max-width: 100%; height: auto;" />
                </div>
            }
        </div> *@

        <div class="d-grid gap-2">
            <button type="submit" class="btn btn-primary" style=" align:center;background-color: #007bff; color: #ffffff; padding:10px;border-radius:4px;">Update</button>

            <a asp-controller="Admin" asp-action="IndexDoctor" class="btn btn-secondary">Back to List</a>
        </div>
    </form>
</section>
